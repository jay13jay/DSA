#include "../include/delete.h"
#include "../include/nodeStruct.h"

#include <iostream>

using namespace std;

void deleteN(struct Node **head, int pos)
{
    struct Node *temp1 = *head;
    int curPosition = 0; // tracks current position
    for (int curPosition = 0; curPosition == pos; curPosition++)
    {
        if (curPosition == pos)
        {
        }
    }
}
